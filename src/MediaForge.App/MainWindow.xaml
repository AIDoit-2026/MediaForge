<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="MediaForge.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MediaForge.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MediaForge"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="MediaForge">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="RootFrame" Grid.Row="1" />

        <Border
            x:Name="ErrorNotification"
            Grid.RowSpan="2"
            Width="380"
            Margin="0,48,20,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource SystemControlErrorTextForegroundBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="16"
            Visibility="Collapsed">
            <Border.RenderTransform>
                <CompositeTransform x:Name="ErrorNotificationTransform" TranslateX="400" />
            </Border.RenderTransform>
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <FontIcon
                    Glyph="&#xEA39;"
                    FontSize="20"
                    Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}" />
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock x:Name="ErrorNotificationTitle" FontWeight="SemiBold" />
                    <TextBlock x:Name="ErrorNotificationDescription" TextWrapping="Wrap" />
                </StackPanel>
                <Button
                    Grid.Column="2"
                    Padding="8"
                    VerticalAlignment="Top"
                    Click="OnCloseErrorNotificationClick"
                    ToolTipService.ToolTip="Close error notification">
                    <FontIcon Glyph="&#xE711;" />
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
